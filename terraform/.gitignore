.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Optional local secret files — prefer TF_VAR_* instead (see repository README)
secrets.auto.tfvars

# Private per-install overlays (not committed): pass as a second -var-file
varfiles/*.local.tfvars
